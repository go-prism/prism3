global:
  imageRegistry: harbor.dcas.dev/docker.io
  postgresql:
    auth:
      postgresPassword: hunter2
      username: prism
      password: password
      database: prism
primary:
  podAnnotations:
    linkerd.io/inject: enabled